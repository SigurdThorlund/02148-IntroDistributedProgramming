package networking.Commands;

public enum ClientCommand {
    CLIENT_JOIN,
    CLIENT_LEAVE,
    CLIENT_START,
    CLIENT_SHOOT,
    CLIENT_DEAD,
    CLIENT_MOVE,
    CLIENT_SYNC_POS,
    SYNC_INIT,
}
